import { KnowledgeItem } from '@/config/knowledge'
import React from 'react'

type WorkKnowledgeSmartStatusDotProps = {
  collectionRef: KnowledgeItem
  onTooltipVisibilityChange: any
  className?: string
}

export function WorkKnowledgeSmartStatusDot({
  collectionRef,
  onTooltipVisibilityChange,
  className,
}: WorkKnowledgeSmartStatusDotProps) {}
