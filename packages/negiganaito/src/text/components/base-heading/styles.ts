import { makeStyles } from '@griffel/react'

export const useStyles = makeStyles({
  root: {
    color: 'inherit',
    fontSize: 'inherit',
    fontWeight: 'inherit',
    outlineStyle: 'none',
  },
})
