'use client'

import React, { useState } from 'react'
import { Default } from '@/components/demo'

export const HomeScreen = () => {
  const [a, b] = useState()
  return (
    <div>
      <Default />
    </div>
  )
}
